spring:
  application:
    name: homestore-api

  datasource:
    url: ${DB_URL:jdbc:sqlserver://localhost:1433;databaseName=HomeStoreDb;encrypt=true;trustServerCertificate=true}
    username: ${DB_USERNAME:homestore_app}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
